import { repository } from '../models/repository.mjs';
import { can } from '../../access.mjs';
import { fail, str, youtube, body } from '../lib/http.mjs';

export async function coursesController({req,res,path,json,user,token}) {
if(path==='/api/courses'&&req.method==='GET'){const courses=repository.visibleCourses(user.id).map(c=>{const q=JSON.parse(c.questions);return {...c,questions:user.role==='trainer'&&c.owner===user.id?q:q.map(({correct,...rest})=>rest),progress:repository.courseProgress(user.id,c.id)||{completed:0,notes:''},attempts:repository.courseAttempts(user.id,c.id)};});return json({courses});}
if(path==='/api/courses'&&req.method==='POST'||/^\/api\/courses\/\d+$/.test(path)&&req.method==='PUT'){
if(!can(user,'courses.manage'))fail(403,'No tienes permiso para crear o editar cursos.');const id=req.method==='PUT'?Number(path.split('/').pop()):null;const old=id?repository.ownedCourse(id,user.id):null;if(id&&!old)fail(404,'Capacitación no encontrada.');const b=await body(req);if(!str(b.title,150)||!str(b.content,50000))fail(400,'Agrega un título y contenido para estudiar.');if(!Number.isInteger(b.minutes)||b.minutes<1||b.minutes>1000||!Number.isInteger(b.pass)||b.pass<1||b.pass>100)fail(400,'Revisa la duración y la nota mínima.');if(!Array.isArray(b.questions)||b.questions.length>50)fail(400,'La prueba admite hasta 50 preguntas.');const questions=b.questions.map(q=>{if(!str(q.text,1000)||!Array.isArray(q.options)||q.options.length<2||q.options.length>6||q.options.some(o=>!str(o,500))||!Number.isInteger(q.correct)||q.correct<0||q.correct>=q.options.length)fail(400,'Cada pregunta necesita opciones y una respuesta correcta.');return {text:str(q.text,1000),options:q.options.map(o=>str(o,500)),correct:q.correct};});if((b.published||old?.published)&&!can(user,'courses.publish'))fail(403,'Necesitas permiso de publicación para modificar cursos publicados.');if(b.published&&!questions.length)fail(400,'Agrega al menos una pregunta antes de publicar.');const fields=[str(b.title,150),str(b.category,80)||'General',str(b.description,600),b.minutes,str(b.content,50000),youtube(str(b.video,500)),b.published?1:0,b.pass,JSON.stringify(questions)];let result;if(id){const revision=old.revision+(old.questions!==JSON.stringify(questions)||old.pass!==b.pass?1:0);repository.updateCourse(...fields,revision,id);}else result=repository.createCourse(...fields,user.id);return json({id:id||Number(result.lastInsertRowid)});}
const match=path.match(/^\/api\/courses\/(\d+)\/(progress|attempt)$/);if(match&&req.method==='POST'){if(user.role!=='learner')fail(403,'Solo los participantes registran avances y resuelven evaluaciones. Usa la vista previa para revisar tu contenido.');const c=repository.publishedCourse(Number(match[1]));if(!c)fail(404,'Capacitación no disponible.');const b=await body(req);if(match[2]==='progress'){const prev=repository.progressRecord(user.id,c.id);repository.saveProgress(user.id,c.id,b.completed===undefined?(prev?.completed||0):(b.completed?1:0),b.notes===undefined?(prev?.notes||''):str(b.notes,10000));return json({ok:true});}
const questions=JSON.parse(c.questions);if(b.revision!==c.revision)fail(409,'La prueba cambió. Vuelve a abrir la capacitación.');if(!questions.length||!Array.isArray(b.answers)||b.answers.length!==questions.length||b.answers.some((a,i)=>!Number.isInteger(a)||a<0||a>=questions[i].options.length))fail(400,'Responde todas las preguntas.');const score=Math.round(questions.filter((q,i)=>q.correct===b.answers[i]).length/questions.length*100);const passed=score>=c.pass;repository.createAttempt(user.id,c.id,score,passed?1:0,JSON.stringify(b.answers),c.revision);return json({score,passed,correct:questions.filter((q,i)=>q.correct===b.answers[i]).length,total:questions.length});}

fail(405,'Método no permitido.');
}
